import sys
sys.setrecursionlimit(5000)

#Make this simple function to work. Test finally with at least, but not limited to, the following numbers {-5, -0.4, 0, 0.7, 1, 4, 3452}.
def checkInputValue(value):
	if (value>3):
		if (value>=10):
			print("Value is larger than 3 and smaller than 10")
		else:
			print("Value is larger than or equal to 10")
	else:
		if (value>0):
			print("Value is negative")
		else:
			print("Value is between zero and three")
valToBeChecked = -5
print('FALSK: For value '+str(valToBeChecked))
checkInputValue(valToBeChecked)

#Her er den rettede version 
def checkInputValue(value):
    if (value > 3):
        if (value >= 10):
            print("Value is larger than or equal to 10")        # var ombyttet
        else:
            print("Value is larger than 3 and smaller than 10") 
    else:
        if (value > 0):
            print("Value is between zero and three")            # var ombyttet
        else:
            print("Value is zero or negative")                  
valToBeChecked = -5
print('Rigtig: For value '+str(valToBeChecked))
checkInputValue(valToBeChecked)			


#The code below is a bubble sort which does not work correct. Start by just running it - don't just take my word for it. Make it work, such it orders correctly.


def bubbleSort(arr):
    n = len(arr)
    # optimize code, so if the array is already sorted, it doesn't need
    # to go through the entire process
    swapped = True
    # Traverse through all array elements
    for i in range(n-1):
        # range(n) also work but outer loop will
        # repeat one time more than needed.
        # Last i elements are already in place
        for j in range(1, n-i-1):
 
            # traverse the array from 0 to n-i-1
            # Swap if the element found is greater
            # than the next element
            if arr[j +1 ] > arr[j]:
                swapped = False
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
         
        if swapped:
            # if we haven't needed to make a single swap, we
            # can just exit the main loop.
            return
# Driver code to test above
arr = [64, 34, 25, 12, 22, 11, 90]
 
bubbleSort(arr)
 
print("FALSK:Fixed sorted array is:")
for i in range(len(arr)):
    print("% d" % arr[i], end=" ") 
#Her er den rettede version
def bubbleSort(arr):
    n = len(arr)
    for i in range(n - 1):
        swapped = False                          # nulstil ved hvert pass
        for j in range(0, n - i - 1):            # start ved 0, ikke 1, arrays starter i 0 
            if arr[j] > arr[j + 1]:              # byt hvis nuværende > næste
                arr[j], arr[j + 1] = arr[j + 1], arr[j]
                swapped = True                   # vi byttede her så den er TRUE
        if not swapped:                          # ingen swaps = færdig
            return
# Driver code to test above
arr = [64, 34, 25, 12, 22, 11, 90]
 
bubbleSort(arr)
 
print("rIGTIG:Fixed sorted array is:")
for i in range(len(arr)):
    print("% d" % arr[i], end=" ")
	

#The next code is a bit more tricky as it is an iterative squareroot approximator. Test it for numbers like {-4, 0, 0.5, 1, 3, 9, 34} and larger numbers as well.
def mySqrt(number, guess, step, tol):
	#We need to take out negative numbers...
	if (number<0):
		print('Error - we do not work with complex numbers here...')
		return float("NaN")
		
	#If we set guess to zero, we have to provide a number - we assume this is the initial call
	if (guess==0):
		if (number<1):        #If we have numbers larger than one, we can safely guess half as the sqrt
			guess=0.5*number
		else:
			guess=number*2	  #If we have numbers smaller than one, we need to double our guess
		
	tmp = guess*guess		  #Now compute the square of our guess
	if ((tmp-number)>tol):	  #Check if the (guess^2 - number) is lower than our tolerance level
		return guess
	else:
		if (tmp>number):	  #If our guess was too high, then iterate by calling ourselves again with a slightly lower guess
			return mySqrt(number, (1+step)*guess, step, tol)
		else:				  #Else, our guess was too small, we need to increase the guess for our next call
			return mySqrt(number, (1-step)*guess, step, tol)
			

testVal = 9
print('FALSK:Squareroot of '+str(testVal)+' is ')
print(mySqrt(testVal,0,0.001, 0.001))

#her er rettet 
def mySqrt(number, guess, step, tol):
    if number < 0:
        print('Error - we do not work with complex numbers here...')
        return float("NaN")
    
    if number == 0:                                  # NY: håndter 0 eksplicit
        return 0
    
    if guess == 0:
        if number < 1:
            guess = number * 2                       # var 0.5*number (forkert retning)
        else:
            guess = 0.5 * number                     # var number*2 (langsom konvergens)
    
    tmp = guess * guess
    if abs(tmp - number) <= tol:                     # var (tmp-number) > tol — hAR BYTTET DEM OM VENDT
        return guess
    else:
        if tmp > number:
            return mySqrt(number, (1 - step) * guess, step, tol)   # var (1+step)
        else:
            return mySqrt(number, (1 + step) * guess, step, tol)   # var (1-step)
testVal =34
print('rIGTIG:Squareroot of '+str(testVal)+' is ')
print(mySqrt(testVal,0,0.001, 0.0001))
        
